# 解題紀錄

## v1-1 AC(0.8s, 304KB)

- 初版
- 使用 io 優化可以把時間壓到 0.2s

## v1-2 AC(0.9s, 300KB)

- 不再每次循環都重新計算 `secret_cnt`，改用複製的
- 使用 io 優化可以把時間壓到 0.2s

> 似乎沒什麼差別

## v2-1 AC(0.8s, 300KB)

- 不再使用 `unordered_map` 計算元素數量，用過的元素就直接修改成 `-1`


## v3-1 AC(0.8s, 300KB)

- 不使用 `std::vector`，改用 `std::array`
- 計算 b 的方式改用數學性質
